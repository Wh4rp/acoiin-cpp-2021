#include <iostream>
using namespace std;

int main() {
  bool condicion = false;
  if(condicion){
    cout << "Entramos en el if, condición verdadera\n";
  }
  else{ // else = si no
    cout << "Entramos en el else, condición falsa\n";
  }

  return 0;
}
